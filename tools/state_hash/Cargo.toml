
[package]
name = "state_hash"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1"
engine = { path = "../../engine" }
tokio = { version = "1", features = ["rt-multi-thread", "macros"] }
hex = "0.4"
